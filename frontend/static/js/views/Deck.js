export default class Decks {
    constructor(params) {
        this.params = params;
        document.title = "View Deck | SpaceReps";
    }

    async getHTML() {
        return ``;
    }

    async main() {
        javascript:void(0);
    }
}